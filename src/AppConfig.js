// Pages
import Dashboard from './pages/dashboard';

// Routing config
const Routes = [
  {
    // index must be last in the array
    path: '/',
    component: Dashboard,
    linkText: 'Dashboard',
  },
];

export { Routes };
